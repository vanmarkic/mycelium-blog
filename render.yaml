services:
  # Node.js app
  - type: web
    name: mycelium-blog-web
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # Secrets are added via dashboard (once, shared across projects)
    
  # Python app (if needed)
  - type: web
    name: mycelium-blog-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app

databases:
  - name: mycelium-blog-db
    databaseName: mycelium-blog
    plan: starter  # $7/month, or 'free' for 90-day trial

# Optional: Background workers, cron jobs, etc.
